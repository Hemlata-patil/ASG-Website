import { test as base } from '@playwright/test';

// Define any custom test fixtures or helpers if needed
export const test = base.extend({
  // Extend basic test with custom behaviors if necessary
});

export { expect } from '@playwright/test';
